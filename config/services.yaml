# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration
parameters:
    locale: 'en'
    img_dir: '%kernel.project_dir%/public/uploads/img'
    img_banner_dir: '%kernel.project_dir%/public/uploads/img/banners'
    default_mail_to: 'lance.l4nce@yandex.ru'
#password: 147852369
services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        public: true       # Allows optimizing the container by removing unused services; this also means
                            # fetching services directly from the container via $container->get() won't work.
                            # The best practice is to be explicit about your dependencies anyway.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/*'
        exclude: '../src/{DependencyInjection,Entity,Migrations,Tests,Kernel.php}'

    # controllers are imported separately to make sure services can be injected
    # as action arguments even if you don't extend any base controller class
    App\Controller\:
        resource: '../src/Controller'
        tags: ['controller.service_arguments']

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    App\Services\CompanyService:
        arguments: ['@doctrine.orm.entity_manager', '@service_container']

    App\Services\ReviewService:
        arguments: ['@doctrine.orm.entity_manager', '@service_container']

    App\Services\UserService:
        arguments: ['@doctrine.orm.entity_manager', '@security.token_storage']

    App\Services\NetworkService:
        arguments: ['@doctrine.orm.entity_manager', '@security.token_storage']

    App\Services\AuthService:
        arguments: ['@doctrine.orm.entity_manager', '@security.password_encoder']

    App\Services\EventService:
        arguments: ['@doctrine.orm.entity_manager']

    App\Services\AdvertService:
        arguments: ['@doctrine.orm.entity_manager', '@service_container']

    App\Services\CommentService:
        arguments: ['@doctrine.orm.entity_manager', '@service_container', '@security.token_storage']

    App\Services\LikeService:
            arguments: ['@doctrine.orm.entity_manager', '@service_container', '@security.token_storage']

    App\Services\CouponService:
        arguments: ['@doctrine.orm.entity_manager']

    AppBundle\Services\EmailService:
        arguments: ['@twig', '@mailer', '%default_mail_to%']


    App\Services\CategoryService:
        arguments: ['@doctrine.orm.entity_manager', '@service_container', '@security.token_storage']

hwi_oauth:
    firewall_names:     ['main']
    resource_owners:
        vk:
            type:                vkontakte
            client_id:          '%env(VK_CLIENT_ID)%'
            client_secret:      '%env(VK_SECRET_KEY)%'