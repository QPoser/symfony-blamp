{% extends 'base.html.twig' %}

{% block title %}Edit profile!{% endblock %}

{% block body %}

    <h2>Edit profile</h2>
    <hr>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" class="btn btn-primary">
    {{ form_end(form) }}

{% endblock %}