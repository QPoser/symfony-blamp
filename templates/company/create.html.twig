{% extends 'base.html.twig' %}

{% block title %}Добавить компанию{% endblock %}

{% block body %}
    <h2>Добавить компанию</h2>
    <a href="{{ path('company') }}" class="btn btn-primary">< Все компании</a>
    <hr>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" class="btn btn-primary" value="Добавить">
    {{ form_end(form) }}
{% endblock %}
