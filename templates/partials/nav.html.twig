<nav class="navbar navbar-expand-md navbar-light bg-white">
    <div class="container">

        {#<a class="navbar-brand#}
                    {#{% if currentPath == path('category.list') %}#}
                    {#active{% endif %}" href="{{ path('category.list') }}">Categories</a>#}

        {#<a class="navbar-brand#}
                    {#{% if currentPath == path('tag.list') %}#}
                    {#active{% endif %}" href="{{ path('tag.list') }}">Tags</a>#}

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="navbar-collapse collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a href="{{ path('company') }}" class="nav-link
                    {% if currentPath == path('company') %}active{% endif %}
                    ">Компании</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('user.index') }}" class="nav-link
                    {% if currentPath == path('user.index') %}active{% endif %}
                    ">Пользователи</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a href="#" id="navbarDropdown" class="nav-link dropdown-toggle" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            {{ app.user.username }} <span class="caret"></span>
                        </a>

                        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                            {% if app.user.isAdmin %}
                                <a href="{{ path('admin') }}" class="dropdown-item">Админ-панель</a>
                            {% endif %}
                            {% if app.user.isBusiness %}
                                <a href="{{ path('cabinet.business.profile') }}" class="dropdown-item">Бизнес-панель</a>
                            {% endif %}
                            <a href="{{ path('cabinet') }}" class="dropdown-item">Кабинет</a>
                            <a href="{{ path('company') }}" class="dropdown-item">Компании</a>
                            <a href="{{ path('logout') }}" class="dropdown-item">Выйти</a>
                        </div>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{ path('login') }}" class="nav-link">Войти</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('register') }}" class="nav-link">Регистрация</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>